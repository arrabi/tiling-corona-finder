{
  "name": "tiling-corona-finder",
  "version": "1.0.0",
  "description": "Equitransitive Square Corona Enumeration",
  "main": "corona-finder.ts",
  "scripts": {
    "start": "ts-node corona-finder.ts",
    "generate": "ts-node generate.ts",
    "build": "tsc",
    "build:web": "npx tsc web-app.ts --outDir . --target ES2020 --lib ES2020,DOM --skipLibCheck",
    "dev": "ts-node corona-finder.ts",
    "serve": "npm run build:web && python3 -m http.server 8000",
    "test": "ts-node corona-finder.test.ts"
  },
  "keywords": [
    "tiling",
    "enumeration",
    "geometry"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.10.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
